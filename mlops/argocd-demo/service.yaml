apiVersion: v1
kind: Service
metadata:
  name: argocd-demo
  namespace: argocd-demo
  labels:
    app.kubernetes.io/name: argocd-demo
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: argocd-demo
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30090
      protocol: TCP
