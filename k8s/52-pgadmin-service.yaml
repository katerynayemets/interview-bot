apiVersion: v1
kind: Service
metadata:
  name: pgadmin
  namespace: interview-bot
  labels:
    app.kubernetes.io/name: pgadmin
    app.kubernetes.io/part-of: interview-bot
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: pgadmin
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30050
      protocol: TCP
