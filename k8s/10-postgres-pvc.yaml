apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
  namespace: interview-bot
  labels:
    app.kubernetes.io/name: postgres
    app.kubernetes.io/part-of: interview-bot
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
